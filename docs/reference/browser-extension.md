# ブラウザ版の固定スナップショット比較

評価日: 2026-09-16

## 結論

固定したブラウザ版には、基本変換と辞書管理の入力例・テスト・保存契約があります。Android 版のコアと接続層を設計する際の参照資産として評価しました。この評価は工数の定量比較ではなく、確認できた資産と適用範囲に基づきます。

特に参考になるのは、入力と期待結果を持つテスト、辞書と保存先の差し替え、再帰登録の状態遷移、辞書更新失敗時の旧版保持です。TypeScript の機械的な Kotlin 変換だけでは、内部カーソル編集や Android 固有の確定・接続・プライバシー要件は満たせません。

具体的な DOM への依存はインターフェースで分離されています。一方、コアは編集先に未確定文字やマーカーを挿入し、そこから読みを取得します。この状態所有の方式は、Android 版の[コア設計](../core-design.md)へそのまま適用できません。

## 参照版と検証結果

ブラウザ版の参照 HEAD は `302baea80cb3be1572bee502401fb7890670514e` です。評価対象は HEAD と、送り仮名処理・テストなどの未コミット変更を含むスナップショットです。対象ファイルの SHA-256 と作業ツリーの状態は[版付き診断結果](browser-extension-20260916-results.json)に記録しています。

参照元を変更せずコピーし、Node.js v24.1.0 で診断しました。

- 対象の試験資産は 23 ファイルです。ケース数は[版付き診断結果](browser-extension-20260916-results.json)を参照します。
- 追加の限定プローブ5件で、Android 仕様との差と編集上の限界を確認しました。これらの成功は「差異が再現した」意味であり、Android 要件への合格ではありません。
- この評価にはカバレッジ、ミューテーションテスト、ブラウザ E2E、製品ビルド・型検査、実機、DDSKK 全機能との差分検証を含めていません。
- `docs/architecture.md` は計画・目標も含むため、そこにある「完全対応」などの記述を実績として数えていません。

## テスト資産の内訳

以下はファイルの主目的で分類した件数です。移植可能な割合や要件達成率ではありません。同じ分類にも Android 向けに期待結果を変更すべきケースがあります。

| 分類 | 件数 | 対象ファイル（`test/unit/`） | 活用方法 |
| --- | ---: | --- | --- |
| 変換・辞書モデル・パーサーなど | 195 | `romaji`、`input-mode`、`core-edge-cases`、`core`、`candidate-metadata`、`composite-jisyo-provider`、`jisyo-parser`、`json-jisyo-parser`、`jisyo-buffer-decoder` | 入力列・辞書・期待結果を抽出し、採用仕様に照合します |
| 登録・再帰登録 | 47 | `registration-mode`、`registration-state-transitions` | 状態遷移と親子の確定範囲を利用します。テストの DOM／HUD 準備は Kotlin へ移しません |
| 保存・辞書更新 | 94 | `indexeddb-jisyo-store`、`user-jisyo-store`、`dictionary-loader`、`system-dictionary-manager`、`cooperative-dictionary-parser` | 失敗・中断・競合・候補順の条件を Android の保存実装に対して再検証します |
| 設定・RPC | 33 | `settings-model`、`remote-stores`、`system-dictionary-upload` | 辞書設定の下書き・公開・失敗の区別を利用します。拡張機能の通信権限は移植対象外です |
| ブラウザ接続・表示 | 118 | `browser-adapter`、`editor-targets`、`content-engine`、`registration-modal` | 取消・選択変更等の試験観点を参考にします。DOM の合格を Android 接続の合格と扱いません |

正例だけでなく、辞書の特殊文字・送りブロックの往復、公開失敗時の旧世代保持、古い revision の拒否、同時初期化、保存後の状態取得失敗の区別をテストしています。この部分は新規開発時に考慮漏れを減らす具体的な資産です。

## 要件ごとの再利用評価

パスは `../skk-browser-extension/` を基準にします。

| Android 要件 | 実装・テストの参考度 | 利用できる資産と必要な変更 |
| --- | --- | --- |
| F01〜F02: モード・ローマ字 | 高い | `src/core/romaji/`、`input-mode/` と92件の `romaji`／`input-mode`／`core-edge-cases`。ローマ字の出力と残余の分離は利用できます。半角カナの独立モード、終端規則、設定の注入は追加が必要です |
| F03〜F04: 基本変換・候補 | 高いが仕様差あり | 送りあり、促音、abbrev、接頭辞・接尾辞、候補送り・戻りの状態を参考にできます。Enter、送り優先、表示数・ラベルは採用仕様へ変更します |
| F05: 見出し語途中編集 | 限定的 | `IEditor` に範囲操作はありますが、コアが編集位置を所有する設計ではありません。内部の読み・送り・残余・カーソルを持つモデルが必要です |
| F06: 登録・再帰登録 | 高いが編集部分は要変更 | `RegistrationMode`、`RegistrationMiniBufferEditor`、登録仕様書・47件のテストが有用です。親子への反映と取消を利用し、範囲編集・Unicode・失敗時の扱いを追加します |
| F07〜F09: 学習・複数辞書・入出力 | 高い | `IJisyoProvider`、`IJisyoStorage`、`IUserJisyoStorage`、候補モデル、SKK パーサー、辞書の世代公開・旧版保持を利用できます。IndexedDB 自体は置き換え、候補の統合・送り順位は U06 に合わせます |
| F10: 学習抑止 | 一部 | 学習の入口は追いやすいですが、Android の入力欄単位の禁止や登録保存抑止を含む契約は別に必要です。入力本文を含む既存のエラーログもそのまま移しません |
| F11: 補完・数値変換 | 小さい | 保存層の `lookupPrefix` は補完の土台になります。コアからの補完操作・動的補完や `#0` 等の数値展開の完成経路は確認できません。追加実装・テストが必要です |
| F12: 入力規則・候補の設定 | 小さい | ローマ字テーブルと候補一覧の概念は利用できますが、現状は静的規則と固定7ラベルです。AZIK、句読点・記号設定、可変ページ、一般キー割り当ては追加です |
| F13: Emacs 編集 | 小さい | `IInputMode` の入力契約は英字・数字・記号・C-j/C-g・Enter/BS が中心です。要求する C-a/e/b/f/n/p/h/d/k、M-b/f の内部編集／入力先編集は新たに設計します |
| F14〜F17: Android 接続・表示・復旧 | 試験観点のみ | ブラウザのキー処理・選択変更・キュー処理は参考になります。InputConnection、キーアップ、IME の再開始、物理キーボード検出・画面表示は Android 固有です |
| F18〜F20: 日付・再変換・通信 | 限定的 | 辞書取得やブラウザ内の同期機構はありますが、Android の再変換や端末間同期とは別です。将来機能の完成実装としては扱いません |

## そのまま移すと問題になる箇所

### 1. 編集先の抽象化と、状態の所有は別です

`src/core/skk/editor/IEditor.ts` はテキスト編集、読みの抽出、候補表示、登録画面、モード変更をまとめています。`MidashigoMode`／`AbbrevMode` は `▽` を挿入し、`extractMidashigo()` で編集先から読みを取り出します。`AbstractKanaMode.insertStringAndShowRemaining` も編集先への挿入と表示を実行します。

Android 版では、読みや `▽` の位置を入力先から復元する必要がないようにします。コアが入力状態を保持し、確定文字・未確定表示・候補の更新を出力します。マーカーの表示と内部状態を分けます。辞書インターフェースの分離は踏襲しますが、広い `IEditor` をそのまま InputConnection のラッパーへ移すことは推奨しません。

### 2. Enter／C-j と送り条件には、確認済みの差があります

| 限定プローブ | ブラウザ側で確認した結果 | Android の採用仕様 |
| --- | --- | --- |
| ひらがな状態で `n Enter` | `ん` と改行 | `ん` の確定のみ |
| ひらがな状態で `n C-j` | 残余が消え、文字は出ません | `ん` を確定します |
| `おおk /大/多/[く/多/]/[き/大/]/` を `Entry.forOkuri('く')` で絞る | 候補順は `大`、`多` | 個人辞書の一致優先で `多く`、次が `大く` |

Enter/C-j は既存の `MockEditor` でコアを実行した結果です。Android アプリや実ブラウザ画面での再現報告ではありません。送り順はパーサー＋Entry の局所検証で、個人辞書の取り込み経路全体を実行したものではありません。それでもこれらをそのまま Android の期待値にできないことは確認できます。

根拠は `henkan/KakuteiMode.ts` の `onEnter`／`onCtrlJ`、`jisyo/entry.ts` の `forOkuri` です。`MidashigoMode.onEnter` と `AbbrevMode.onEnter` にも改行挿入があります。`content.ts` が Enter イベントを抑止していても、コア内の改行挿入がなくなるわけではありません。

### 3. 登録ミニバッファは、途中編集・Unicode の完成実装ではありません

`RegistrationMiniBufferEditor` を、外部の `ITextTarget` を付けない内部バッファとして実行しました。

- `😀` を挿入して `deleteLeft()` を呼ぶと、UTF-16 の上位サロゲート `0xd83d` が残ります。実装は `slice(0, -1)` です。
- `東京` の先頭1文字を `replaceRange` で `京` に置き換える操作は、`京京` ではなく `東京京` になります。範囲を使わず末尾に追加しています。

これは内部バッファの API を直接呼んだ確認です。全ブラウザ UI の操作経路で同じ問題が出ると断定するものではありません。Android 版の F05／F06／N08 を満たす部品として、無変更で採用できない根拠です。

### 4. テスト用の編集実装にも意味のある処理があります

`test/unit/mocks/MockEditor.ts` は、文字列・カーソル・マーカー・候補の確定や文字種変換を実装しています。モードのテストはこの編集実装との組み合わせの合格であり、コアだけで全状態を所有できる証明ではありません。登録の47件も実際には BrowserEditorAdapter／HUD と DOM の代替実装を使います。

Android では、キー列を渡して「内部状態・確定出力・辞書要求」を観測できるテストへ移します。モック側へ変換ロジックを再実装しすぎないようにします。

### 5. 共通化の境界と設定を作り直す部分があります

- `EditorFactory` は共有の静的状態を持ち、テストで差し替えています。コンストラクターで `IEditor` を渡す経路もあるため、全面的に依存注入できないわけではありません。Android では入力セッションごとの明示的な依存を使います。
- `RomajiInput` は `romKanaBaseRule` を直接参照します。規則データは参考になりますが、設定したテーブルをセッションへ渡す契約が必要です。
- `MenuHenkanMode` は `asdfjkl` と7件を固定しています。Android の可変候補数・選択キー変更は追加設計です。
- `Entry.onCandidateSelected` はエラーに見出し語を含めてログ出力します。Android の入力本文をログに残さない方針に合わせます。
- 32個の core TypeScript ファイルに完成した機能が集約されているとは扱いません。辞書管理・設定・接続は core の外にもあり、再利用範囲はモジュール単位で判断します。
