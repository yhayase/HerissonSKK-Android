# 初回追加機能の独立レビューと指摘検証

2026-09-20。開始点は `8b3052d`、対象は未コミットの初回追加機能です。作業中の記録であり、公開受入の完了を示しません。

## 分担

- アプリ別設定の実装: Terra / medium。
- 設定・入力の独立レビュー: Sol / medium。状態・保存・画面の複数条件を扱うためです。
- タッチ入力とネットワーク辞書の実装: それぞれ別のSol / medium。
- 設定・入力レビュー指摘の妥当性検証: ネットワーク辞書担当が、担当外のコード・要件から独立判定。
- 通信・永続化境界のレビュー: Astra / low。信頼境界の判断を含むためです。
- 通信レビュー指摘の妥当性検証・画面受入確認: 別のTerra / medium。

レビュー担当と実装担当、指摘検証担当を区別します。モデル名や件数は合格の証拠とせず、再現条件・実差分・実行結果を記録します。

## 採用した指摘

| ID | 再現条件・影響 | 独立判定と対応 |
| --- | --- | --- |
| SET-01 | アプリ別設定の保存中に新しい入力欄へ入ると、後から保存に失敗しても未保存の編集設定がセッションに残る | 妥当。入力経路は保存成功済みの値だけを参照し、保存中・失敗・再試行と新規セッションの回帰試験を追加 |
| SET-02 | 全体設定を継承するアプリで、編集が実際に有効か画面から分からない | S08に照らして妥当。全体・標準・保存済み実効値を表示 |
| CFG-01 | 全体でEmacs無効時に許可されたキー競合を、アプリ別有効化時に再検証すると入力開始が例外になる | 妥当。有効化画面で競合を拒否し、後日のキー変更も入力開始時に確認して編集を無効化・理由表示。既存の全体無効設定を一律に不正データへ変更しない |
| TOUCH-01 | 回転・外部選択変更後、描画側と入力セッションの世代が異なり、新しい候補タップも拒否される | 妥当。世代の整合性と古い候補の拒否を修正し、単体回帰試験を追加 |
| TOUCH-02 | 旧セッションの確認後にサービスの現在の接続へタッチ編集し、入力先切替の競合で別欄を変更する | 妥当。入力セッション所有の接続経路へ集約し、旧接続・新接続の回帰試験を追加 |
| TOUCH-03 | `A😀B`の絵文字直後で左移動するとUTF-16のサロゲート間へカーソルを置く | 妥当。通常欄は既存のUnicode編集境界、保護欄は入力先のネイティブ移動へ修正 |
| TOUCH-04 | 設定読込中・接続失敗時にCtrl+SpaceでIMEを切り替えられない | 妥当。引用入力を優先したうえで、正常なSKKセッションに依存しない切り替えへ修正し、失敗・引用の回帰試験を追加 |
| NET-01 | 取得失敗後に処理中状態が解除されず、再試行や下書き適用ができない | 別担当が妥当と確認。失敗後の復旧操作と試験を追加。取消・破棄後に成功結果が遅着する場合も保存しないことを確認 |
| NET-02 | S辞書の導入ボタンと受信側のIntent extra名が異なる | メイン担当も独立に不一致を発見。共有定数へ修正済み |
| NET-03 | 大文字のHTTPSスキームは取得できるが、保存時の小文字限定検証で失敗する | 別担当が妥当と確認。保存・バックアップとの整合性を修正。追加コア試験の失敗はレコード順序の不正であり、検証コードを変えず試験データを修正 |

## 不採用・未確認

- カテゴリ移動で非表示の下書きが失われるという懸念は不採用です。全項目の保持と回転・保存の試験を追加しています。
- アプリ別編集値がキーマッパーとセッションで異なるという懸念は、共通の設定スナップショットを渡すコードから不採用です。
- 継承へ戻す保存失敗の再試行が失われるという懸念は、失敗対象のパッケージ名で操作有無を区別できるため不採用です。
- 同じ登録深度で候補参照が再利用される古いタップの問題は、到達可能な操作列が未確認です。世代・表示更新の試験で扱います。
- 通信レビューでは入力本文や個人辞書の通信経路、TLS検証の無効化を認めませんでした。ただしDNS事前検証は接続先IP固定の証拠ではなく、実サーバー・端末での取消や容量上限試験を代替しません。

## 検証状況

初回の統合単体試験は631実行中8失敗でした。新しいビュー構造と保護欄表示に対する既存試験の期待、旧SQLiteの模擬スキーマ、文字倍率の試験条件を修正しています。修正後の統合ゲートと実IME・実SQLiteの再実行は継続中です。未実施を成功扱いにしません。

## 非同期入力の追加レビュー

Sol / medium の独立レビューで、タッチによる保留中の編集に続く文字・Enter が先行編集を失わせる問題と、アクション未指定欄へ DONE を発行する問題を確認しました。非同期入力境界の判断と再修正を要したため、Astra / low へ上げて修正しました。別の Sol / medium が修正をレビューし、さらに別の Astra / low が次の指摘を検証しました。

- 辞書待機中の操作列に上限がない指摘は妥当です。先着1,024操作を保持し、超過を通知して受け付けない実装と、終了時破棄の試験を追加しました。
- `IME_FLAG_NO_ENTER_ACTION` の欠落は妥当です。サービスからセッションへ保持し、通常・保護欄、単一行・複数行の各条件でアクションを抑制する試験を追加しました。
- 独立した物理入力が保留編集を無効化する指摘は、現行の版管理による安全性契約に照らして採用しません。入力先へ通過する物理キーを一律に遅延できないため、タッチ内の順序保存とは分け、境界の回帰試験を追加しました。

上記の限定単体試験は96件成功しました。API 35のタッチ実操作試験では、キーの存在を確認できても座標タップ後の動作が期待と異なる4件の失敗が残り、実画面・入力経路を調査しています。単体試験の成功をタッチ実操作の合格には転用しません。

## 統合試験の到達点

`build/reports/local-verification/20260919T170119103802Z/result.json` は成功しました。ホスト26件、core 280件、Android単体678件、debug/release lint、各APKビルドとmanifest検査を含みます。その後の表示修正は、この記録の検証範囲に含めません。

API 26では既存入力18件、目的別設定E2E 1件、実SQLite・クラッシュ復旧・完全バックアップ22件、公式S辞書の実HTTPS取得からSQLite再オープン後の検索1件、前回配布APKからの更新保持が成功しました。SQLite版3を期待する端末試験2箇所と、版1入力の復元後に版1出力を期待するクラッシュ試験は現行の版4・出力版2へ合わせました。旧入力データとデータ同一性の検証は保持し、別担当が妥当性を確認しました。

実通信試験が通常の計装試験に混ざる指摘は、別担当が妥当と確認し、`networkLive=true` の明示引数がある場合だけ実行する条件を追加しました。取得後の検索はフォールバック辞書を渡さないSQLiteだけで確認します。辞書画面の一連の操作の代替とはしません。

## 実画面と入力手順の追加検証

API 35で4件のタッチE2Eが成功しました。画面遷移中に取得した座標で押していた試験を、描画待ち後の座標タップへ変更しました。保護欄で「候補」という部分一致を使うと常設の「前候補」キーを誤検出するため、候補状態のTextViewだけを判定します。キー・候補への座標入力を別のクリック経路へ置き換えていません。

IME下端のナビゲーション余白について、Sol / mediumのレビューでは二重計上が疑われました。別のSol / mediumがAPI 35の実座標と対象版のフレームワークを確認し、不採用としました。修正前は24pxのジェスチャー領域とボタンが重なり、修正後はその上に配置されます。API 26の試験端末はハードウェアナビゲーションのため余白は0です。合成insetの単体試験だけで実ウィンドウの適合を証明できない点は採用しています。

API 26の狭幅240dp・文字倍率2.0の表示失敗は、試験エディターの旧版向けdecor listenerが292pxのリサイズ後に292pxのpaddingを追加し、入力欄を8pxへ縮める問題でした。別担当がコードと実座標から検証し、API 30未満は標準decor処理へ戻しました。同じ処理を使う設定画面も揃えます。候補の重なり判定を緩めず、修正後4件が成功しました。

人間向け受入票も別担当がレビューし、さらに別担当がコアの実装と照合しました。大文字で読み開始、Spaceで候補表示、候補タップは即確定、選択中の取消は読みへ戻ってから再取消、外部カーソル移動は既存表示を一度保持する契約へ操作例を修正しました。

API 35の設定画面では、入力中の欄がy561〜607、キーボード上端がy381となり完全に隠れる不具合を確認しました。別担当が座標・対象版のinset契約を検証し、画面下端からのシステムバー距離とIME距離の最大値を使う修正を採用しました。修正後は入力欄y335〜381、キーボード終了後のviewport下端y616を実画面で確認しています。

記号面に不足していたASCII記号15種類は、高さを増やさない第2記号面へ追加しました。Terra / mediumが実装し、別のTerra / mediumが独立レビューしました。英字へ直接戻れること、記号面が文字入力で切り替わらないこと、セッション開始時のリセットを確認しています。

## 最終確認

最終統合ゲート `20260919T172352284178Z` はホスト26件、core280件、Android単体685件とlint・全APK・権限検査に成功しました。そのAPKを固定したAPI 26/35の端末試験は各32件成功し、API 26のタッチは横画面640×320を含みます。文字入力後にモードキーの表示が初期値へ戻る問題も別担当が検証し、再構築時に現在モードを再表示する修正と回帰試験を追加済みです。

配布用に用意した開発署名付きrelease構成APKそのものでは、API 35の保護入力・通常編集2件と公式S辞書の実取得1件が成功しました。元のAPK・設定・IMEへの復元も成功しています。成果物と全結果は `build/distributions/release-trial-20260920/` に保存しています。未コミットのソース一式と差分も同梱し、本番署名・公開・実機受入は行っていません。
