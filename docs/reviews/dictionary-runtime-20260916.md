# 辞書の公開・入力接続レビュー

履歴資料です。対象時点の判定を保持しています。現在の仕様・残件との区別は [レビュー履歴の案内](README.md) を参照します。

2026-09-16。SQLite保存、辞書管理器、Android接続を分けて確認しました。

辞書管理器は単一のバックグラウンド実行キューで保存と読込を行い、不変の検索スナップショットを一度に公開します。キー処理は公開済みのメモリーだけを検索します。初回未準備・失敗は未登録語と区別し、コアが読み・残余・内部カーソルを復元します。再読込失敗では既存の検索世代を残し、保存成功後の公開失敗は保存失敗と区別します。

rootのレビューで、解除済み監視者への保留通知、close後の公開・通知、呼出元による辞書順リスト変更を修正しました。close後の新規監視も拒否します。辞書管理器8件、利用不能時のコア復元2件で確認しました。

別担当がランタイムとサービス接続を独立に読み、Application contextの利用、主スレッドへの状態通知、監視解除、入力セッション単位の辞書注入、限定同梱辞書の最下位順位を確認しました。状態通知は表示だけを更新し、入力接続へ文字を挿入しません。パスワード・TYPE_NULLのコア迂回は維持しています。

コア93件、Android単体91件は失敗・エラー・スキップ0件です。後者は画面のファイル上限・ピッカー状態試験も含み、API別呼出しを独立要件数とは扱いません。辞書ランタイム接続後の既存入力E2E12件はAPI26・30・35で成功しました。SAF画面から取り込んだ辞書がIME変換へ反映される経路は別途検証中です。

現在は有効辞書を起動・更新時に全読込する方式です。大規模辞書のメモリー・性能目標達成、登録・学習・削除、プロセス中断試験は未完了です。検索自体は同期のメモリー参照なので、非同期検索の完了通知競合を検証済みとは扱いません。
