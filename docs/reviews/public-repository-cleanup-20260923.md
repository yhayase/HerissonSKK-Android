# リポジトリ公開前の整理（2026-09-23）

## 方針

所有者はリポジトリを Public にし、このリポジトリの GitHub Pages からプライバシーポリシーを公開する方針を選択しました。不要ファイルと構成の整理を先行します。この作業では可視性変更、Pages 公開、push、履歴書換えは行っていません。

## 整理した内容

- ルートに残った未追跡の `info:-` は、参照のない ImageMagick の MIFF 出力でした。内容を保持して Git 管理外の `build/repository-cleanup-20260923/imagemagick-info.miff` へ移しました。移動前後の SHA-256 は `cf0c8f28f5990215bfb93073ccb542180edbb91ecd81e9fcf45f2ac3c61fc26a` で一致します。一時保存であり、ビルドの清掃時に失われてもよい生成物です。
- README の公開準備・SDK・AGP の記載を現行構成へ更新し、各ディレクトリの役割を記載しました。
- 文書案内へ Play 準備とポリシーを追加し、レビュー一覧の未掲載記録と監査資料の案内を補いました。
- DDSKK の固定版と検証結果を保持し、3文書の個人環境に依存した絶対パスを汎用的な説明・変数へ変更しました。過去のコマンドを移植可能な表記へ変更したことを明示しました。
- `app/`、`core/`、`test-editor/`、`scripts/` の役割は分かれているため、ビルド構成を変える移動はしていません。ルートのアイコン原本と Android リソースは用途が異なるため保持します。
- ライセンス監査 JSON、試験資料、レビュー履歴は判断の根拠として保持します。ビルド成果物、SDK パス、署名鍵などは既存の除外規則で Git 管理外です。

## 独立した確認

構成・不要ファイルのレビューと、秘密情報・履歴の調査を別担当が実施しました。絶対パスの置換と MIFF の退避は、別担当も参照版・監査記録を損なわない整理と確認しました。

履歴調査はローカルブランチと remote-tracking ref から到達する49コミット、1,013個の異なる blob を対象としました。追跡対象371パスと除外されていない未追跡ファイルも確認しました。秘密鍵・主要サービスのトークンのパターンに一致する確実な秘密情報、履歴内の署名鍵・辞書データベース・APK・`local.properties` は検出していません。広い認証情報検索の一致は試験用の合成データでした。画像の出典メタデータは保持しました。

この調査は秘密情報が存在しないことの証明ではありません。到達不能・reflog のみのオブジェクト、リモートのみにある参照、無視対象のファイル、画像の視覚内容、任意形式の秘密情報は網羅していません。過去の絶対パスは履歴に残りますが、認証情報ではないため、それだけを理由とする履歴書換えは行いません。

## 検証と残件

追跡対象と未追跡の Markdown の相対リンク先、レビュー一覧への掲載漏れ、`git diff --check` を確認し、欠落・エラーはありませんでした。製品コードの変更を含まない整理のため、この整理だけを理由とするビルド・機能試験の再実行は行いません。

Public 化と Pages の配信設定・公開 URL の確認は未実施です。並行して進めていた [API 36 検証の残件](api36-20260923.md)も維持します。
