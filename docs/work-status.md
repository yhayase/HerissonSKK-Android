# 開発の継続記録

更新日: 2026-09-16

## 現在の作業

既存のフェーズ1を再検証しながら、純 Kotlin コアのローマ字処理と基本変換を実装しています。全製品の完成やフェーズ1の実機性能合格はまだ宣言していません。

GitHub は `yhayase/skk-android` のプライベートリポジトリです。開始前の文書・評価資料を `6510fb5` でコミット・pushしました。以後も検証済みの単位でpushします。

## 今回の検証

- `:app:testDebugUnitTest`: 53実行、失敗・エラー・スキップ0件です。追加した接続の3テストをAPI26/35で実行しました。
- `python3 -B -m unittest discover -s scripts -p '*_test.py'`: 5件成功です。
- `python3 scripts/test-emulator.py --serial emulator-5580`: API30の専用エミュレーターで接続E2E3件成功、スキップ0件です。
- 実行器の修正前は `OK (4 tests)` に性能試験の assumption skip が含まれていました。接続試験のクラスを明示選択し、スキップ・途中終了・件数不整合を成功としない判定へ修正しました。
- 独立QAが追加の接続テスト、実行器、実行器の5テストをレビューしました。これらへの具体的な不具合指摘はありませんでした。
- 設計レビューで候補ラベルと q/Q の競合が指摘され、適用前の検証と拒否を設計へ反映しました。
- 計測時の入力欄準備待ちを修正し、独立レビュー後にAPI30でスモークと通常30／30／1,000回を完走しました。`9b55f32` をpush済みです。
- 純JVMの内部編集部品は独立レビューで5件のUnicode不具合が再現しました。ICU4J77.1への置換・削除後の境界再評価・CRLF処理で修正し、別担当が元の再現例を再実行して解消を確認しました。
- 編集部品14テストと、Unicode16公式1,093ケースを検査する適合テスト1件が成功しました。件数の厳密な確認を追加した後も再実行済みです。
- Android試験APKへ同じICUを組み込み、API30で接続E2E3件＋Unicode実行試験2件が成功、スキップ0件です。IME本体への新コア統合はこれからです。

## 残件と次の手順

1. ローマ字部品の独立レビューと、基本変換エンジンの実装・試験を進めます。
2. API26用システムイメージを準備し、最低対応版でのUnicode実行・接続試験を確認します。
3. Android接続・候補UIへ統合し、要件からのE2Eを拡充します。完全なテーブル・辞書のライセンス確認は複製前に行います。
4. Xiaomiでの性能測定の原因解消は実機再実行まで未確認です。

ユーザーから今は実機接続できないため実装と自動試験を先行する指示を受けました。実機が必要な検証を合格にせず、独立して進められる作業を続けます。詳細な試験不足は [対応表](test-coverage.md)、技術判断は [コア設計](core-design.md) を参照します。
