# 開発の継続記録

## 現在の計画と履歴の区別

2026-09-19に利用者と合意した公開範囲・順序を [ロードマップ](roadmap.md) へ反映しました。これは文書のみの変更です。初回の追加必須機能を実装済みとは扱いません。以下の各日付の記録・件数・未実施事項は当時の証拠であり、今後の計画はロードマップ、現在の残件は [試験対応表](test-coverage.md) を参照します。

更新日: 2026-09-19

## 2026-09-19 の候補全文ボタンと固定案内への対応

実装ソースは `021c62f`、実機確認用は `build/distributions/021c62f/app-debug.apk` です。SHA-256 は `10b1b53b682d6c89e72b109dccbd0cbb9e0c7db4da169570bd92dcd9e6757a7c` です。前回 `a1d748a` と同じ開発署名で上書き更新できます。

「候補の全文」は、一覧で選択中の候補または注釈が文字数上限・表示幅によって省略された場合だけ表示します。他の候補だけが省略されても表示せず、選択や画面幅の変化で省略がなくなると全文表示も閉じます。単独インライン候補には表示しません。「ローカル辞書・限定試験辞書」の固定案内を削除し、読み込み中・失敗などの状態通知は維持しました。[表示契約](candidate-display-design.md) に記録しています。

独立レビューで同一ページ内の選択変更と実際の幅による省略の検証不足を指摘し、修正・再確認しました。未コミット変更のないソースのローカルゲート `20260919T081602770722Z` は、コア278件・Android単体591実行・ホスト26件、lint・APK検査が成功し、失敗・エラー・スキップはありません。

最終APKでAPI 26／35のネイティブ表示各4件・文字倍率2の候補表示E2E各4件が成功しました。短い候補でのボタン非表示、実際の省略に応じた表示・消去、候補選択と確定を検証しました。レポートのソース・APKハッシュの一致と端末設定の復元を確認し、配布ディレクトリの `manifest.json` に対応を保存しました。Xiaomi Pad 8での今回の実機受入は未実施です。

## 2026-09-19 の文書構造・現行契約の整理

[文書案内](README.md)で正本の担当範囲を整理し、利用手順をREADMEから分離しました。コア・登録・辞書・補完・バックアップ・表示の古い説明を現行実装と照合し、試験件数は本書の履歴へ集約しました。レビュー文書の元の指摘・判定は変更せず、対象時点の履歴と明示しています。実装変更・新たなアプリ試験・リリース判定は行っていません。

## 2026-09-19 のストレージ主体の辞書検索への対応

実装ソースは `a1d748a`、実機確認用は `build/distributions/a1d748a/app-debug.apk` です。SHA-256 は `5468041d3174d7e03168d80b188629b3658fb48c0f4dee562b270de94a0e451e` です。前回 `1b9faa2` と同じ開発署名で上書き更新できます。設定・辞書の保存形式は変わらず、移行や再取り込みは不要です。

SQLiteを正本とし、起動・学習後の全件読み込みとメモリー索引構築を廃止しました。必要な見出し・補完範囲だけを取得し、LRUを128検索・4,096レコード・524,288 UTF-16文字以内に制限します。一見出しが上限を超える場合も変換でき、操作中だけ結果を保持して終了時に解放します。学習のSQLは対象見出しだけを更新します。キー配送中にディスクを待たず、未取得時はワーカーで読み、入力順と取消・入力先の境界を検証して再実行します。

同一API 26エミュレーターの合成10万見出しで、旧版の辞書準備778.993 ms・学習775.680 msに対し、配布版は1.379 ms・2.620 msでした。通常の未キャッシュ検索は0.687 ms、キャッシュ検索p95は0.044 msです。OSキャッシュが温まった条件であり、実機の起動時間ではありません。[測定条件](dictionary-performance.md) に限界を記載します。

未コミット変更のないソースからローカルゲート `20260919T001602830652Z` が成功しました。コア278件、Android単体587実行、ホスト26件に失敗・エラー・スキップはありません。lintとdebug／release／benchmark APK・通知・権限検査も成功しました。キャッシュ各上限、未登録語の追い出し、対象外行への書き込み禁止、世代・抑止・復元、数値変換、遅延結果とキー順を検証しました。[設計契約](dictionary-storage-cache-design.md) と [独立レビュー](reviews/bounded-dictionary-cache-20260919.md) に記録しています。

最終APKでAPI 26の実SQLite・障害復旧22件、完全バックアップ1件、実プロバイダー障害1件、辞書性能・処理量1件が成功しました。API 26／35の入力各18件・設定各1件・拡大文字候補表示各4件、API 35のSAF辞書・学習1件も成功しました。配布APKのハッシュと各レポートの一致、端末設定の復元を確認し、配布ディレクトリの `manifest.json` に対応を保存しました。Xiaomi Pad 8、Firefox・X・Keepでの今回の実機受入は未実施です。プライベート作業ブランチへpushし、ドラフトPR #1を更新しました。mainへはマージしていません。

## 2026-09-17 の横並び候補・インライン注釈・送り認識への対応

実装ソースは `1b9faa2`（本体 `41d5423` と表示通知・座標監視の修正）、実機確認用は `build/distributions/1b9faa2/app-debug.apk` です。SHA-256 は `3d7f884ab54c8c87963e58bdb7b14889fcb3a0a439ce3cd7c2e3667be6e2ed39` です。前回 `bc1e24a` と同じ開発署名で、アンインストールせず上書き更新できます。

候補一覧を横並びの最大2段へ変更し、注釈を小さく淡くしました。「入力規則・句読点・候補」の「一覧の候補数」で「画面幅と文字サイズに合わせる」を選ぶと、1段に収まる件数を使います。既存の固定設定は「上限を指定」と表示し、設定件数までを幅・高さに応じて最大2段に並べます。

単独候補の注釈は入力先が通知するカーソル座標を使って、候補の近くのIME所有ウィンドウに表示します。下部へ候補情報を重複表示せず、注釈の文字はアプリ本文へ送信しません。座標通知に対応しない入力先では注釈を省略します。`KasU` は `KaSu` と同じ送りあり変換になります。

未コミット変更のないソースからの増分ビルドで、ローカルゲート `20260917T095600315551Z` が成功しました。コア269件、Android単体558実行、ホスト26件に失敗・エラー・スキップはありません。lint、debug／release／benchmark APKと通知・権限検査も成功しました。件数は網羅率を意味しません。

実IME試験では、注釈の表示位置・タッチ後の入力フォーカス・確定や入力欄切替での消去と、アプリ本文への非混入を検証しています。候補タイルの可視範囲、ページ移動、拡大文字・横画面を実測し、画像も確認しています。検証中に見つかった本文方式の注釈残留、Android 8の読み上げ情報の遅れ、選択中IMEの上書き導入後の接続不整合、座標監視要求によるキー処理の停止は [原因・設計・独立レビュー](reviews/horizontal-inline-okuri-20260917.md) に記録しました。元の入力・ページ移動の検査を弱めず修正しています。

最終APKの通常入力はAPI 26／35で各17件、入力設定は各1件成功しました。API 26のSAF辞書1件、API 26／35の拡大文字候補表示各4件、API 35の横画面候補表示4件も成功し、設定の復元を確認しました。API 26では辞書操作に続けて表示試験を行いました。各レポートのAPKハッシュは配布物と一致し、対応を配布ディレクトリの `manifest.json` に保存しました。Xiaomi Pad 8、Firefox・X・Keepでの今回の受入は未実施です。設定・辞書の保存形式は前回の版4を維持します。プライベートリポジトリの作業ブランチと [ドラフトPR #1](https://github.com/yhayase/skk-android/pull/1) へpushし、mainへはマージしていません。

## 2026-09-17 の引用・学習・標準ローマ字・候補一覧への対応

修正ソースは `bc1e24a`、実機確認用は `build/distributions/bc1e24a/app-debug.apk` です。SHA-256 は `2694b2e538b2abc9f2c4e18a75e106d7a0a63c663424ada3aa6acbaef9cf4770` です。前回 `f279fab` と同じ開発署名を確認しており、アンインストールせず上書き更新できます。

C-q による次キーの引用、任意の ▽／▼ 表示、確定直後の学習順位、DDSKK の z 系記号と fa/fi/fe/fo、候補一覧の表示とページ送りに対応しました。矢印は指定どおり `zh← zj↓ zk↑ zl→` です。[仕様・原因・独立レビュー](reviews/quote-learning-menu-20260917.md) を参照します。

入力設定は版4で保存します。旧標準の C-q は引用へ移行し、半角カナ切替は未割当となります。入力規則設定で再指定できます。他のカスタム割当は保持します。表示記号は同画面の「未確定文字に ▽／▼ を表示する」で有効にします（既定オフ）。読戻し未対応の欄では表示せず、外部変更・接続断で除去を照合できない場合は本文保護のため記号が残る場合があります。

未コミット変更のない `bc1e24a` からの増分ビルドで、ローカルゲート `20260917T073149711933Z` が成功しました。コア265件、Android単体536実行、ホスト22件に失敗・エラー・スキップはありません。lint、debug／release／benchmark APK、通知・権限検査も成功しました。件数は要件の網羅率を意味しません。

API 26／35 で通常入力16件、設定1件、SAF辞書1件がそれぞれ成功しています。設定試験は引用キーの押下・反復・解放、表示記号の確定時除去、標準記号、既存の編集・WebViewを含みます。SAF試験は二本の確定直後の再変換と保存・再読込を含みます。API 35 の通常入力・設定・SAF は配布APKとハッシュが一致しています。API 26 の通常入力・設定・SAF は最終の検証エラーメッセージ修正前のAPKで成功し、配布APKでも拡大文字の候補E2Eが成功しています。

候補表示は実際の描画画像も確認し、API 35 では通常の7行、文字倍率2で5行、横画面で4行、API 26 の文字倍率2で4行をスクロールなしで表示しました。可視性の試験はレイアウト完了を待ち、Androidがクリップした画面座標だけで合格しないよう、行本来の高さとも比較します。Nativeの巨大本文表示2件もAPI 35で成功しました。設定・画面状態は実行器で復元しています。各レポートとAPKの対応は配布ディレクトリの `manifest.json` に記録しています。

プライベートリポジトリの作業ブランチと [ドラフト PR #1](https://github.com/yhayase/skk-android/pull/1) へ反映しました。Xiaomi Pad 8、Firefox、X、Keep と物理キーボードでの今回の受入は未実施です。UIの全面改善は引き続き [マイルストーン #1](https://github.com/yhayase/skk-android/milestone/1) で扱います。

## 2026-09-17 の編集互換性の追加報告への対応

修正ソースは `f279fab`、今回の実機確認用 APK は `build/distributions/f279fab/app-debug.apk` です。最終行の C-p/n、末尾の C-h/d、周辺文字取得に制約のある入力欄、C-m 後の継続編集、M-</> の不要な範囲選択、候補ゼロ件の登録取消を修正しました。[原因・判断・独立レビュー](reviews/editor-compatibility-20260917.md) に記録しています。

未コミット変更のない `f279fab` の増分ビルドで、ローカルゲート `20260917T060634320143Z` が成功しました。コア 260 件、Android 単体 485 実行、ホスト 22 件で、失敗・エラー・スキップはありません。lint、debug／release／benchmark の APK・通知・権限検査も成功しました。件数は網羅率を意味しません。

最終 APK の通常入力 E2E は API 26／35 で各 16 件成功しました。記録は `test-editor/build/reports/connection/` 以下の API 26 `20260917T060713477129Z`、API 35 `20260917T060718924248Z` です。

設定・編集 E2E は API 26／35 それぞれ同じ APK で連続2回成功しました。通常 EditText、取得なし・抽出のみ・絶対位置不明・改行変換の接続、ローカル WebView の textarea/contenteditable を含みます。M-</> は Shift/Alt を離す前に選択の両端を確認します。記録は `test-editor/build/reports/customization-e2e/` 以下の API 26 `20260917T060439730050Z`／`20260917T060450892609Z`、API 35 `20260917T060341878825Z`／`20260917T060425152589Z` です。設定と IME の復元も全回成功しています。これらはコミット前の実行ですが、配布 APK と SHA-256 の一致を確認しました。

前回 `81cb7ee` と同じ開発署名を確認しました。今回の変更は辞書・設定の保存形式を変更せず、アンインストールせず更新できます。Firefox、X、Keep の実アプリと Xiaomi Pad 8 での受入は未実施です。UI 全面改善は引き続き [マイルストーン #1](https://github.com/yhayase/skk-android/milestone/1) の対象です。

専用ブランチ `fix/input-dictionary-followup-20260917` と [ドラフト PR #1](https://github.com/yhayase/skk-android/pull/1) へ反映し、main へはマージしていません。

## 2026-09-17 の追加報告への対応

修正ソースは `81cb7ee`、検証用 APK は `build/distributions/81cb7ee/app-debug.apk` です。再帰登録の C-g、3候補目からの一覧と開始位置設定、かな記号、変更のない設定画面の終了、新しい編集キーとリピート・文末移動、辞書編集の一括適用に対応しました。[仕様・判断・レビュー](reviews/input-dictionary-followup-20260917.md) を参照します。UI 全体の見直しは [マイルストーン #1](https://github.com/yhayase/skk-android/milestone/1) とし、今回には含めません。

ローカルゲート `20260917T050755766642Z` はコア 258 件、Android 単体 441 実行、ホスト 22 件、lint と APK 検査がすべて成功しました。失敗・エラー・スキップはありません。最終ソースをコミットする前の増分ビルドで、APK とソースの対応は配布台帳へ記録しています。

最終 APK の入力 E2E は API 26／35 各 15 件、設定・新規キー E2E は API 26 で 1 件、Native SQLite は API 35 で 22 件、SAF の一括辞書適用は API 35 で 1 件成功しました。API 35 の新規キーも最終の辞書通知修正前に成功しています。前回配布 APK `7398c40` からの独立 fixture の更新保持は API 26 で成功しました。実機での新しい操作感は未確認です。

自動承認レビューが main への直接 push を拒否したため、安全な代替として `fix/input-dictionary-followup-20260917` ブランチへ push しています。main へはマージしていません。

## 2026-09-17 の実機報告への対応

候補が入力欄を覆う問題を、文字キーのない input frame による領域確保と通常表示の高さ固定で修正しました。公式 L 辞書の正規の注釈・見出しを拒否していた解析も修正し、失敗理由と画面再作成時の中断を表示します。C-b は Emacs 編集オフによる仕様どおりの配送であることをユーザーが確認しました。有効化の説明を補いました。[原因・独立レビュー・検証の記録](reviews/device-feedback-20260917.md) を参照します。

修正コミット `7398c40` のローカルゲート `20260917T012947172046Z` は、コア 252 件、Android 単体 401 実行、ホスト 22 件が成功しました。失敗・エラー・スキップはなく、lint と APK 構築・配布物検査も成功しました。未コミット変更のないソースからの増分ビルドです。公式 L 辞書 175,789 見出しの実 SQLite への取り込み・再読込は API 35 で成功しました。表示回帰試験は旧配布 APK で遮蔽を検出し、修正版で成功しています。修正版の実機受入は未完了です。

以下は 2026-09-16 時点の記録です。

表示・更新保持・プロバイダー障害のチェックポイント `2a7eca2` はプライベート GitHub へ push 済みです。基本設定の保存結果・再試行・回転時の結果保持と、保存失敗時にも学習を再許可しない処理を追加しました。[独立レビューと試験](reviews/basic-settings-20260916.md) を参照します。

## 2026-09-16時点の状態

必須の入力、辞書、設定、完全バックアップと、長文候補の有界表示を実装しました。同梱辞書は限定試験データです。要件別の証拠と残る条件は [対応表](test-coverage.md)、表示・更新・配布境界の判断は [レビュー](reviews/display-upgrade-distribution-20260916.md) に記録します。実機性能や対象アプリ・物理配列の受入合格は宣言していません。

2026-09-16 の現行作業ツリーに対するローカルゲート `20260916T072203657752Z` では、コア JVM 250 件、Android 単体 393 実行、ホスト 22 件がすべて成功しました。lintDebug／lintRelease、debug／release／benchmark APK の通知・権限検査も成功しました。件数は要件の網羅率を表しません。コードの最終修正 `f025b9d` は push 済みです。生成物のないクリーンチェックアウトでも `20260916T072547824347Z` に同じ件数が全件成功し、失敗・エラー・スキップは 0 でした。

専用エミュレーターで、同署名旧版 `17a5cf0` からの独立 fixture の更新保持を API 26／30／35 で各 1 件確認しました。`app/build/reports/upgrade-persistence/` の履歴は順に `20260916T065655000139Z`、`20260916T065226018685Z`、`20260916T065743946027Z` です。旧 APK の SHA-256 と署名、現行 APK の情報は各 `preflight.json` にあります。[検証範囲と保護手順](upgrade-validation.md) を参照します。

長文候補の Native 表示試験は API 26 `20260916T065741182030Z`、API 35 `20260916T065338124029Z` で各 2 件成功しました。候補表示と物理キーの実 IME 試験も API 26 `20260916T072250204202Z`、API 30 `20260916T072048718341Z`、API 35 `20260916T072049157427Z` で各 2 件成功しました。API 30 の文字倍率は 1.3、他は 2.0 です。別プロセスの実 ContentProvider が正常なバックアップ本文の後に読取エラーを返す試験は API 26 `20260916T070752447180Z`、API 35 `20260916T070755346942Z` で各 1 件成功しました。実プロバイダーによる出力先 close 障害は未検証で、単体の障害注入だけが証拠です。

通常入力 E2E は API 26／30／35 で各 13 件、現行変更に対する最新の接続レポートで全件成功しました。辞書 SAF E2E も最終設定コードで API 26 `20260916T072328893485Z`、API 30 `20260916T071956856229Z`、API 35 `20260916T072342165430Z` に各 1 件成功しました。履歴は `test-editor/build/reports/saf-dictionary-e2e/` にあります。物理キーボード設定案内と辞書別状態のレビュー修正はコンパイル・単体検証を通過しましたが、端末での再確認とは区別します。

入力設定・AZIK・Emacs の最終 E2E も API 26 `20260916T072047917031Z`、API 30 `20260916T072250749091Z`、API 35 `20260916T072250580450Z` で成功しました。

クリーン APK でも API 26 の入力 13 件、API 35 の入力 13 件・設定 1 件・表示とキーボード操作 2 件・辞書 SAF 1 件が成功しました。結果は `build/reports/clean-verification/f025b9d/`、APK は `build/distributions/f025b9d/` に保存しました。増分ビルドとの DEX 差分があるため、両者は別の SHA-256 を持つ成果物として記録しています。

実装・独立レビュー・現在の開発環境で実施する自動検証を完了しました。Xiaomi Pad 8 の実打鍵、US／JIS 配列、対象アプリ、実辞書性能は利用可能な環境で確認します。API 35 の Chrome は初期利用手続きが未完了で、利用規約の同意操作は行っていません。本番署名・プロジェクトの公開ライセンス・公開の判断は所有者の作業です。将来機能は必須項目の未完了として数えません。

## 過去の段階記録

以下は各段階で記録した結果と当時の残件です。現在の判定には上の状態と [要件対応表](test-coverage.md) を使います。GitHub は `yhayase/skk-android` のプライベートリポジトリで、文書 `6510fb5`、登録 `9629c54`、学習など `5e469d4`、入力設定 `17a5cf0` まで push した履歴があります。

### 初期段階の検証

- 補完追加時点のコアJVM試験は184件、Android単体試験は178件が成功しました。失敗・エラー・スキップは0件です。Android件数はRobolectricの複数API実行を含み、独立した要件数や網羅率を表しません。
- コアにはUnicode16公式コーパス1,093ケース、生成入力32 seed・16,000操作、生成登録24 seed・16,800操作、生成削除24 seed・16,800操作、動的補完の有無の差分比較24 seed・16,800操作と遅延完了検証を含みます。
- Python実行器の試験5件が成功しています。失敗・スキップ・途中終了・件数不整合を成功にしません。
- IME、確認アプリ、instrumentation APKをビルドしました。IMEのlintは指摘0件です。ICUライセンスを同梱しています。
- Native SQLiteはAPI26・35各8件が成功しました。実容量不足、破損ファイル保持、再接続、文字コード往復、未コミット中の専用子プロセス終了、v1→v2移行、抑止復元を検証します。
- SAFの実IME E2EはAPI26・30・35で各1件成功しました。一つのシナリオで退避、置換、変換、通常学習と別セッション再変換、学習禁止時の順位保持、物理キー登録、数値8形式の連続変換、削除確認・抑止・復元、動的補完の未受諾とRight受諾、書出し、元データのバイト一致復元と設定復元を検査します。

この段階のSAF成功履歴は `test-editor/build/reports/saf-dictionary-e2e/` 以下です。

- API26: `emulator-5582/20260916T044742940596Z`
- API30: `emulator-5580/20260916T044705422191Z`
- API35: `emulator-5584/20260916T044755632253Z`

この段階のNative SQLite成功履歴は `app/build/reports/dictionary-instrumentation/` 以下です。

- API26: `emulator-5582/20260916T043034476076Z`
- API35: `emulator-5584/20260916T043057901111Z`

履歴にはAPKのSHA-256、API、ソースコミット、未コミット変更の有無を記録します。上記は未コミット変更を含む検証結果であり、クリーンコミットのビルド結果とは区別します。SAFは成功時に元の個人辞書とIME選択を復元します。復元失敗時は成功扱いにせず、UUID名のバックアップと復旧情報を保持します。

通常の入力E2EはAPI26・30・35各13件です。Enter送信境界、入力欄切替、外部カーソル、送り・abbrev・接辞、内部編集、一覧と注釈、保護欄、Unicodeを対象にします。固定候補の試験欄は標準の学習禁止フラグを使い、実行順の順位干渉を防ぎます。補完接続後も3 API各13件すべて成功しました。

### 初期段階のレビューと修正

- Unicode境界、ローマ字終端、変換取消・残余順序、遅延した自分自身の選択通知による座標巻戻しを独立レビューで修正しました。選択通知はガードを外す変異試験でも回帰検出を確認しました。
- 登録では複数文字入力の漏出、取消、子登録後のモードを修正しました。[登録レビュー](reviews/registration-20260916.md)を参照します。
- 学習は確定成功後だけ要求し、保護欄・学習禁止・確定拒否では保存しません。古い接続の完了通知は新しい入力欄に作用しません。[学習・復旧レビュー](reviews/learning-recovery-20260916.md)を参照します。
- 数値変換では大字の萬、直積の重複、リテラル候補の誤除外、正規化検索と登録キーの不一致を修正しました。反復修正のため担当をstandardからcomplexへ上げ、別担当が確認しました。
- 数値登録は保存要求時の辞書で展開して確定文を固定します。要求前の再公開は反映し、要求後は再計算しません。「登録開始時から世代固定が必要」というレビュー指摘は契約に照らして誤検知と判定し、指摘担当も確認しました。実Managerの再公開を使う境界試験を追加しました。
- 抑止をManagerで落とす接続漏れを修正し、取り込み辞書と組み込み辞書の両方を試験しました。
- API26のSAFハーネスは古いDocumentsUIのtoolbarを識別するよう修正しました。API30の数値試験で、試験側のsetTextと遅延選択通知が次の打鍵に競合したため、外部書換えを使わない連続変換へ修正しました。修正後は3 APIで成功しています。

### 当時の残件と次の手順

1. 候補削除はAPI26・30・35のSAF E2Eで成功しました。API26のSwitchに状態文字列が付く差に試験側で対応し、復旧を確認して再実行しました。候補削除の[独立レビュー](reviews/candidate-deletion-20260916.md)を記録しました。
2. 補完・動的補完の[独立レビュー](reviews/completion-20260916.md)と実IME試験を完了し、設定可能な入力規則とキー、Emacs入力先編集の設計を進めています。全対象アプリ・物理配列の互換性は残件です。
3. 非同期照会の要求番号・待機操作、実辞書の容量・性能、更新時データ保持、完全バックアップ形式などの未完了範囲は[対応表](test-coverage.md)へ記録します。
4. Xiaomi Pad 8の性能・Bluetooth実打鍵は接続できるまで未確認です。ユーザーの指示に従い、実装と自動試験を先行します。

過去のAPI30性能試験は `performance/20260916T022606Z` で30／30／1,000回成功し、API35スモークは `20260916T022959Z` で3／3／5回成功しました。入力再接続の準備競合を `2ec2dd8` で修正済みです。辞書・登録・学習追加後の性能測定はまだであり、実機受入基準の達成を意味しません。プロセス終了試験も電源断の証拠ではありません。

この段階の入力E2E成功履歴（`test-editor/build/reports/connection/`）:

- `emulator-5580/20260916T044245683970Z`
- `emulator-5582/20260916T044337307771Z`
- `emulator-5584/20260916T044246906067Z`

### カスタマイズ基盤の進捗

補完の `ea70107` をプライベートGitHubへpush済みです。続いてコンパイル済みローマ字規則、句読点、候補ラベル・固定ページ、純粋なUnicode編集計画を追加し、コア205件・Android単体178実行が成功しました。[基盤レビュー](reviews/customization-foundation-20260916.md)を参照します。この時点では設定保存・画面・カスタムキー・AZIK・外部編集は未接続でした。以下が接続後の結果です。


### 入力設定・AZIK・Emacs 編集の接続結果

2026-09-16、設定画面、世代競合を検出する AtomicFile 保存、セッション開始時の設定固定、キー割当、AZIK、内部・外部編集を接続しました。独立レビューの指摘と修正は [統合レビュー](reviews/customization-integration-20260916.md) に記録します。

中央検証ではコア 241 件・Android 単体 316 実行が全件成功（失敗・スキップ 0）、lint とアプリ・instrumentation APK のビルドも成功しました。ホスト側ランナー試験は 13 件成功です。件数を目標値にはせず、要件と境界の対応を確認します。

入力設定 E2E は設定保存・初期化・新規入力先への反映、AZIK とキー変更、Emacs 無効時の配送、有効時の絵文字・CRLF・上下移動・削除を実 IME で確認し、3 API すべて成功しました。各履歴は `test-editor/build/reports/customization-e2e/` 以下です。

- API 26: `emulator-5582/20260916T060350650602Z`
- API 30: `emulator-5580/20260916T060410629111Z`
- API 35: `emulator-5584/20260916T060320200471Z`

各試験は開始時の設定ファイルの有無とバイト列を退避し、終了時に照合して復元しました。旧ランナーの `exec-out` が API 26 の失敗出力を内容と誤認した問題は、`shell -T` と明示的な存在検査、復元前の全ハッシュ照合に修正しました。旧履歴の復元記録は独立した手動復旧記録で訂正し、上記 3 回は修正版による検証です。

連続編集では入力先の表示更新が完了通知より先行する競合を再現し、実行中 1 件と待機 64 件の順序付き処理へ修正しました。待機列はコマンド種別だけを保持し、各操作は新しい本文から計画します。独立レビューと競合の回帰試験を通過しています。標準 InputConnection の最終照合と入力先の変更を原子的にできない制約は残ります。

完全辞書バックアップは [設計](complete-backup-design.md) の独立レビューを終え、実装中です。実機、実アプリとの互換性、更新時保持、長い候補と画面倍率の検証は引き続き残件です。


### 完全辞書バックアップの実装と検証

入力設定の `17a5cf0` はプライベート GitHub へ push 済みです。続いて完全辞書バックアップのストリーム形式、全件検証と専用 staging、SQLite v3 移行、revision 競合、全件の原子的復元、保存済み・未公開の再読込、入力セッションの旧書込許可の拒否、SAF 画面を実装しました。[独立レビュー](reviews/complete-backup-20260916.md) で 4 指摘を修正しています。

中央検証はコア 250 件・Android 単体 352 実行、ホスト 13 件が全件成功しました。lint と APK ビルドも成功しています。全件数には各 SDK での同一シナリオの実行が含まれ、網羅率とは扱いません。

Native SQLite は既存 8 件から、完全復元・コミット前後の別プロセス終了・詳細な v2 移行を追加した 21 件へ増えました。API 35 の成功履歴は `app/build/reports/dictionary-instrumentation/emulator-5584/20260916T062534821402Z` です。API 26 でも 21 件が `emulator-5582/20260916T062639387836Z` で成功しました。

完全バックアップの実 SAF E2E は、日常用 DB を使わず分離した DB と UUID ファイルで保存→内容変更→全件復元を実行します。個人候補、システム順序、無効状態、抑止を照合し、選択取消・確認取消・不正入力による無変更を確認しました。成功履歴は `app/build/reports/complete-backup-saf-e2e/` 以下です。

- API 26: `emulator-5582/20260916T062543010098Z`
- API 30: `emulator-5580/20260916T062648613210Z`
- API 35: `emulator-5584/20260916T062954189125Z`

辞書性能測定は API 30 の `app/build/reports/dictionary-performance/emulator-5580/20260916T061901Z` で最小 4 件・1,000 件・100,000 件を各 256 回検証しました。100,000 件の検索 p95 は通常 0.016 ms・送り 0.013 ms・数値 0.026 ms・補完 0.026 ms、学習後の再構築・公開は 842.667 ms でした。検索は同期のメモリー経路を維持し、バックグラウンド再構築の待機コストを性能上の検討対象として残します。実機合格値ではありません。

この段階で未完了だった自動検証は、実プロバイダー由来の I/O 障害、同署名旧版 APK からの更新保持、長い候補・画面倍率・狭い幅の操作、配布物とクリーン環境の最終検査でした。その後の成果は冒頭の「現在の状態」に記録します。この時点の表示レビューでは候補ビューの高さ無制限と AUTO 幅の取り方に具体的な問題を確認しました。
